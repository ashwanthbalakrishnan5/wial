---
import { getChapterData } from "@repo/template-core/lib/data";

const slug = import.meta.env.CHAPTER_SLUG;
const { chapter } = await getChapterData(slug);
const defaultLocale = chapter.default_language;
---

<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="refresh" content={`0;url=/${defaultLocale}/action-learning`} />
    <link rel="canonical" href={`/${defaultLocale}/action-learning`} />
    <title>Redirecting...</title>
  </head>
  <body>
    <p>Redirecting to <a href={`/${defaultLocale}/action-learning`}>/{defaultLocale}/action-learning</a>...</p>
  </body>
</html>
